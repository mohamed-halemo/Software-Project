///FlashMode Enum, used for a state variable that configures the flashmode listener
enum UserFlashMode {
  always,
  auto,
  never,
}

///BrushMode Enum, used for a state variable that rebuilds the widget depending on the edting mode
enum BrushMode {
  Saturation,
  Exposure,
  Contrast,
  Brightness,
  Crop,
  Rotate,
}
